/**
 * Our club's identity in the interface.
 *
 * The crest is hotlinked from the federation rather than committed to this
 * repository: the repository is public and the crest is not ours to
 * redistribute, which is the same reason `public/hero.jpg` is left out.
 *
 * If HNS ever moves the file the badge falls back to the lettermark, so a dead
 * link costs a bit of polish rather than a broken page.
 */
export const OUR_CLUB_CREST_URL =
  'https://hns.family/files/images_comet/Club/_resized/515_1734293597_100_100_wg_t.png';

/** Shown when no crest loads - the club's initial in club colours. */
export const OUR_CLUB_INITIAL = 'H';
